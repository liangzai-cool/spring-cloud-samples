spring:
  application:
    name: spring-cloud-eureka-consumer

server:
  port: 8081

management:
  context-path: /admin

eureka:
  instance:
    statusPageUrlPath: ${management.context-path}/swagger-ui.html
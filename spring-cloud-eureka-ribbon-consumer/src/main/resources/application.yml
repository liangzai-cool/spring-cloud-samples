spring:
  application:
    name: spring-cloud-eureka-ribbon-consumer

server:
  port: 8082

management:
  context-path: /admin

eureka:
  instance:
    statusPageUrlPath: ${management.context-path}/swagger-ui.html